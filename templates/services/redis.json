{
  "name": "redis-service",
  "description": "Redis in-memory database service",
  "commands": {
    "install": [
      "docker pull redis:7-alpine"
    ],
    "setup": [
      "mkdir -p ~/wsdata/redis/data",
      "docker volume create redis_data"
    ],
    "run": "docker run -d --name redis --network {{NETWORK_NAME}} -p 6379:6379 -v redis_data:/data redis:7-alpine redis-server --appendonly yes"
  },
  "variables": ["NETWORK_NAME", "IP"],
  "ports": ["6379:6379"],
  "volumes": [
    "redis_data:/data"
  ],
  "accessUrl": "redis://{{IP}}:6379",
  "dependencies": [],
  "isCore": false
}