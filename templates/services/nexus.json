{
  "name": "Nexus Repository OSS",
  "description": "Universal artifact repository manager",
  "commands": {
    "install": [
      "docker pull sonatype/nexus3:latest"
    ],
    "setup": [
      "mkdir -p ~/wsdata/nexus/data"
    ],
    "run": "docker run -d --name nexus --network {{NETWORK_NAME}} -p 8085:8081 -v ~/wsdata/nexus/data:/nexus-data -e INSTALL4J_ADD_VM_PARAMS='-Xms512m -Xmx512m -XX:MaxDirectMemorySize=512m' --restart unless-stopped sonatype/nexus3:latest"
  },
  "variables": ["NETWORK_NAME"],
  "dependencies": []
}
