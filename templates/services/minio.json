{
  "name": "Minio Object Storage",
  "description": "S3-compatible object storage server",
  "commands": {
    "install": [
      "docker pull minio/minio:latest"
    ],
    "setup": [
      "docker network create {{NETWORK_NAME}} || true",
      "mkdir -p ~/wsdata/minio/data"
    ],
    "run": "docker run -d --name minio --network {{NETWORK_NAME}} -p 9001:9001 -p 9090:9090 -v ~/wsdata/minio/data:/data -e MINIO_ROOT_USER=admin -e MINIO_ROOT_PASSWORD={{DATABASE_PASSWORD}} minio/minio server /data --console-address \":9001\""
  },
  "variables": ["NETWORK_NAME", "DATABASE_PASSWORD"],
  "dependencies": [],
  "accessUrl": "https://minio.{{DOMAIN}}"
}