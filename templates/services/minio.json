{
  "name": "Minio Object Storage",
  "description": "S3-compatible object storage server",
  "commands": {
    "install": [
      "docker pull minio/minio:latest"
    ],
    "setup": [
      "docker volume create minio_data"
    ],
    "run": "docker run -d --name minio --network {{NETWORK_NAME}} -p 9001:9001 -p 9090:9090 -v minio_data:/data -e MINIO_ROOT_USER=admin -e MINIO_ROOT_PASSWORD={{STORAGE_PASSWORD}} minio/minio server /data --console-address \":9001\""
  },
  "variables": ["NETWORK_NAME", "STORAGE_PASSWORD"],
  "volumes": [
    "minio_data:/data"
  ],
  "environment": {
    "MINIO_ROOT_USER": "admin",
    "MINIO_ROOT_PASSWORD": "{{STORAGE_PASSWORD}}"
  },
  "dependencies": [],
  "accessUrl": "https://minio.{{DOMAIN}}"
}