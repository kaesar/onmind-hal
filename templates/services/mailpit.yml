name: Mailpit
description: Email testing tool for developers - fake SMTP server with web UI
commands:
  install:
    - docker pull axllent/mailpit:latest
  setup:
    - mkdir -p ~/{{DATA_PATH}}/mailpit
  run: |-
    docker run \
          -d \
          --name mailpit \
          --network {{NETWORK_NAME}} \
          -p 1025:1025 \
          -p 8025:8025 \
          -v ~/{{DATA_PATH}}/mailpit:/data \
          -e MP_DATA_DIR=/data \
          --restart=always \
          axllent/mailpit:latest
variables:
  - DATA_PATH
  - NETWORK_NAME
dependencies: []
