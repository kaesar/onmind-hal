{
  "name": "RabbitMQ",
  "description": "Message broker for distributed systems",
  "commands": {
    "install": [
      "docker pull rabbitmq:3-management"
    ],
    "setup": [
      "mkdir -p ~/wsdata/rabbitmq/data",
      "docker volume create rabbitmq_data"
    ],
    "run": "docker run -d --name rabbitmq --network {{NETWORK_NAME}} -p 5672:5672 -p 15672:15672 -v rabbitmq_data:/var/lib/rabbitmq -e RABBITMQ_DEFAULT_USER=admin -e RABBITMQ_DEFAULT_PASS={{STORAGE_PASSWORD}} rabbitmq:3-management"
  },
  "variables": ["NETWORK_NAME", "STORAGE_PASSWORD"],
  "dependencies": []
}
